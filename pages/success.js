import Layout from "../components/layout";

const Success = () => {
    return (
        <Layout>
            <div>
                Thank you for reaching out! Someone will be in touch with you
                shortly.
            </div>
            ;
        </Layout>
    );
};

export default Success;
